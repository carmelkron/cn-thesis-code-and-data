# Python dependencies for the offline part of the thesis.
# Install with:  pip install -r requirements.txt
#
# Versions are left unpinned deliberately: the runs reported in the thesis were
# made against the model APIs below rather than against a fixed local stack, and
# the analysis code does not depend on any version-specific behaviour.

# Agent framework and model access
smolagents
litellm
groq
python-dotenv

# Data handling
pandas
numpy
openpyxl          # required by pandas for the .xlsx files
pyyaml            # agent configuration in Refined_CN_Agents/

# Analysis and statistics
scipy
statsmodels
scikit-learn
scikit-posthocs   # Dunn post-hoc tests
matplotlib
seaborn
tqdm

# Safety analysis only (refinement-per-claim/safety_metrics/)
detoxify
transformers
torch

# Field experiment only (field-experiment/notebooks/)
xgboost                   # the ML section of main_effect.ipynb
shap                      # feature attribution for that model
requests
google-generativeai       # poster_features_build.ipynb, needs GEMINI_API_KEY
# xpoz                    # poster_features_build.ipynb also calls the XPOZ API.
#                           Not on PyPI; supplied by XPOZ. That notebook's
#                           output is committed, so every downstream analysis
#                           runs without it.